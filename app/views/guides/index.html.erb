<p id="notice"><%= notice %></p>

<h1>Guides</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Country</th>
      <th>Hobby</th>
      <th>Instagram</th>
      <th>Description</th>
      <th>Profile image</th>
      <th>Position</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @guides.each do |guide| %>
      <tr>
        <td><%= guide.first_name %></td>
        <td><%= guide.last_name %></td>
        <td><%= guide.country %></td>
        <td><%= guide.hobby %></td>
        <td><%= guide.instagram %></td>
        <td><%= guide.description %></td></td>
        <td><%=  cl_image_tag(guide.profile_image, :width => 100, :height => 150, :crop => :fill)%>
        <td><%= guide.position %></td>
        <td><%= link_to 'Show', guide %></td>
</td>
        <td><%= link_to 'Destroy', guide, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Guide', new_guide_path %>
