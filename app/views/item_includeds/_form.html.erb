<%= form_with(model: item_included, local: true) do |form| %>
  <% if item_included.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_included.errors.count, "error") %> prohibited this item_included from being saved:</h2>

      <ul>
        <% item_included.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
